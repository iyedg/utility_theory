from setuptools import find_packages, setup

setup(
    name="src",
    packages=find_packages(),
    version="0.1.0",
    description="Utility function to evaluate regional differences with regards to health indicators",
    author="Iyed Ghedamsi",
    license="MIT",
)
